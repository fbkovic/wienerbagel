# Database
DATABASE_URL=postgresql://user:password@host:port/database

# Environment
NODE_ENV=production

# Optional: Session Secret (falls du Session-basierte Auth nutzt)
# SESSION_SECRET=dein-geheimer-session-schlüssel
